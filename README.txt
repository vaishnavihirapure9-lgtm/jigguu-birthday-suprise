# Jiggu's 19th Birthday Website 🎀

Letter photos:
- The full school-uniform photo is shown without cropping, so both faces remain visible.
- The second photo also keeps its original proportions.
- On mobile the photos stack naturally.

Song:
- The supplied Kalank audio is clipped from about 3:47, where the requested "Main tera..." section begins, through the end of the supplied track.
- The on-screen lyrics use the exact text you provided, with updated timing for the full section.

To replace photos, use the same filenames inside assets.
To replace the song section, replace assets/kalank_final.mp3.
